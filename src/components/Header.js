// img import
import logo from "../assets/scss/img/logo.png";

const Header = () => {
    return (
        <header>
            <img src={logo} alt="Bifrost" />
        </header>
    );
};

export default Header;
